# All the gems we've installed and used so far
source 'https://rubygems.org'

gem 'sinatra'
gem 'pg'
gem 'bcrypt'
gem 'httparty'
gem 'sinatra_generator'

# Run bundle -> that installs everything inside an already installed gem (and won't install if the gem wasn't already installed)